# Client Scripts

Keep Godot input, rendering, synchronization, prediction, interpolation, and diagnostics scripts here.

Run the phase 3 algorithm tests without opening a window:

```powershell
& "F:\godot\Godot_v4.7.1-stable_win64_console.exe" --headless --path client `
  --script res://tests/phase3_algorithms_test.gd
```
