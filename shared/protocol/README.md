# Shared Protocol

[`protocol.md`](protocol.md) defines the current v2 UDP header, handshake, movement input, and world snapshot messages. Keep protocol specifications, message identifiers, compatibility notes, and byte-level test vectors here. Generated language bindings may be added later, but generated build artifacts should remain ignored.
