# Shared Protocol

[`protocol.md`](protocol.md) defines the current v1 UDP header and `Hello`/`HelloAck`/`Ping`/`Pong` messages. Keep protocol specifications, message identifiers, compatibility notes, and byte-level test vectors here. Generated language bindings may be added later, but generated build artifacts should remain ignored.
