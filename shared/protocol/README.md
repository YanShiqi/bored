# Shared Protocol

Keep protocol specifications, message identifiers, compatibility notes, and byte-level test vectors here. Generated language bindings may be added later, but generated build artifacts should remain ignored.
