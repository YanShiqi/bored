# C++ Game Server

This directory will contain the authoritative fixed-tick C++ server, CMake configuration, and tests. The first implementation should accept UDP input commands and broadcast world snapshots without account, database, or deployment dependencies.
